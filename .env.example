# Ejemplo de configuración de variables de entorno
# Copia esto a .env si vas a correr localmente

# Puerto del servidor (Railway lo asigna automáticamente)
PORT=3000

# Entorno
NODE_ENV=development

# (Opcional) Directorio de descargas
# DOWNLOAD_DIR=/home/usuario/Downloads/youdown_downloads
